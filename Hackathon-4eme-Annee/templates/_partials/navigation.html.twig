<nav class="navbar navbar-expand-lg navbar-light bg-light{% if top is defined %}is-top{% endif %}">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('front_index') }}"><img src="{{ asset('build/images/logo.png') }}" alt="logo" height="40"> Les agents du web</a>
        <button id="btn-sidebar" class="btn btn-transparent text-white p-0">
            <i class="fas fa-chevron-left"></i>
        </button>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="ml-auto navbar-nav">
                {% if app.request.requestUri starts with '/admin' %}
                    {% if app.user %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <img src="{{ asset('build/images/pictos/mon-espace_jaune.svg') }}" alt="Mon compte" height="30"><span>{{ 'user.title'|trans }}</span>
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <a class="dropdown-item" href="{{ path('back_profile_index') }}"><i class="fas fa-user"></i>{{ 'profile.plurial'|trans }}</a>
                                <a class="dropdown-item" href="{{ path('back_profile_edit') }}"><i class="fas fa-user-cog"></i>{{ 'user.informations'|trans }}</a>
                                <a class="dropdown-item" href="{{ path('security_logout') }}"><i class="fas fa-sign-out-alt"></i>{{ 'button.logout'|trans }}</a>
                            </div>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('security_login') }}"><i class="fas fa-sign-in-alt"></i>{{ 'button.login'|trans }}</a>
                        </li>
                    {% endif %}
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('security_login') }}"><img src="{{ asset('build/images/pictos/mon-espace_jaune.svg') }}" alt="Mon compte" height="30">{{ 'button.space'|trans }}</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>