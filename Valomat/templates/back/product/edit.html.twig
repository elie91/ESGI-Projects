{% extends 'back/index.html.twig' %}

{% block title %}{{'product.list.edit'|trans}}{% endblock %}

{% block content %}
    <section class="mt-3">
        <div class="container">
            <div class="row align-items-center mb-3">
                <div class="col-md-6">
                    <h1>{{'product.list.edit'|trans}}</h1>
                </div>
                <div class="col-md-6 text-right">
                    <a class="btn btn-primary" href="{{ path('back_product_index') }}"><i class="fas fa-arrow-left"></i>{{'button.back'|trans}}</a>

                    {{ include('back/product/_delete_form.html.twig') }}
                </div>
            </div>
            <div class="card">
                <div class="card-body">
                    {{ include('back/product/_form.html.twig', {'button_label': 'button.save'}) }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
