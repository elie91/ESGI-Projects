{% extends 'back/index.html.twig' %}

{% block title %}{{ 'featureValue.button.create'|trans }}{% endblock %}

{% block content %}
    <section class="mt-3">
        <div class="container">
            <div class="row align-items-center mb-3">
                <div class="col-md-6">
                    <h1>{{ 'featureValue.button.create'|trans }}</h1>
                </div>
                <div class="col-md-6 text-right">
                    <a class="btn btn-primary" href="{{ path('back_feature_index') }}"><i class="fas fa-arrow-left"></i>{{'button.back'|trans}}</a>
                </div>
            </div>
            <div class="card">
                <div class="card-body">
                    {{ include('back/feature_value/_form.html.twig') }}
                </div>
            </div>
        </div>
    </section>


{% endblock %}
