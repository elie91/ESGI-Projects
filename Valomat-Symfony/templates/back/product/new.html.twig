{% extends 'back/index.html.twig' %}

{% block content %}
    <section class="mt-3">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="row">
                        <div class="col-md-6">
                            <h1>{{ 'product.button.add'|trans }}</h1>
                        </div>
                        <div class="col-md-6 text-right">
                            <a class="btn btn-primary sm-mb-1" href="{{ path('back_product_index') }}"><i class="fas fa-arrow-left"></i>{{'button.back'|trans}}</a>
                        </div>
                    </div>
                    <div class="card">
                        <div class="card-body">
                            {{ include('back/product/_form.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

{% endblock %}
